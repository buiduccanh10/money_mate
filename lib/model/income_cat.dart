class income_cat {
  String? cat_id;
  String? name;
  String? icon;
  bool? is_income;

  income_cat(
      {required this.cat_id,
      required this.name,
      required this.icon,
      required this.is_income});
}
