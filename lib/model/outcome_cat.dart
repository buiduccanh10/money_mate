class outcome_cat {
  int? cat_id;
  String? name;
  String? icon;
  bool? is_income;
  outcome_cat(
      {required this.cat_id,
      required this.name,
      required this.icon,
      required this.is_income});
}
